# ====================================================================
# NEXUS — Wymagania Python
# Platforma AI do tworzenia krótkich wirusowych wideo
# Optymalizacja: OpenAI-first, najtańszy koszt, najwyższa jakość
# ====================================================================

# --- Rdzeń FastAPI (z WebSocket i wysoką wydajnością) ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
uvloop==0.21.0               # Szybsza pętla asyncio (2-3x)
httptools==0.6.4              # Szybszy parser HTTP
websockets==14.1              # WebSocket support
python-multipart==0.0.18
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.0

# --- OpenAI (klucz główny) ---
openai==1.59.9

# --- Multi-agent Orchestration (LangGraph) ---
langgraph==0.2.62
langchain==0.3.14
langchain-core==0.3.28
langchain-openai==0.2.14
langsmith==0.2.3

# --- RAG (Retrieval-Augmented Generation) ---
chromadb==0.6.3
faiss-cpu==1.9.0.post1
tiktoken==0.8.0

# --- Przetwarzanie wideo i audio ---
ffmpeg-python==0.2.0
Pillow==11.1.0
numpy==2.2.1
scipy==1.14.1

# --- Analityka i ML ---
scikit-learn==1.5.2
pandas==2.2.3

# --- Bazy danych ---
sqlalchemy==2.0.36
asyncpg==0.30.0
redis==5.2.1
redis[asyncio]==5.2.1         # Async Redis (WebSocket pub/sub)
aiohttp==3.11.11

# --- Bezpieczeństwo i Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# --- Celery + Flower (async background tasks) ---
celery[redis]==5.4.0          # Celery z Redis backendem
flower==2.0.1                 # Celery monitoring dashboard
kombu==5.4.2                  # Celery transport layer

# --- Monitoring i logi ---
structlog==24.4.0
prometheus-client==0.21.1
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0

# --- HTTP klient ---
httpx==0.28.1
aiofiles==24.1.0

# --- Narzędzia deweloperskie ---
pytest==8.3.4
pytest-asyncio==0.24.0
black==24.10.0
ruff==0.8.3
mypy==1.13.0

# --- Pomocnicze ---
tenacity==9.0.0
python-slugify==8.0.4

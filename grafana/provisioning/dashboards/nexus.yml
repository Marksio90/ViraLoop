apiVersion: 1

providers:
  - name: NEXUS Dashboards
    folder: NEXUS
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
